ezgin_kafka:
  xxx: # 唯一标识，用于获取连接时查找使用, 必填
    servers: #ip:port 集群内多个服务器之间用逗号分隔，必填
    ack:  #ack模式 no,local,all, 默认all
    auto_commit:   #是否自动提交 true, false, 默认true
    partitioner:   #分区选择模式 hash,random,round-robin, 默认hash
    version: #kafka版本, 默认2.8.1
  yyy: # 唯一标识，用于获取连接时查找使用, 必填
    servers: #ip:port 集群内多个服务器之间用逗号分隔，必填
    ack:  #ack模式 no,local,all, 默认all
    auto_commit:   #是否自动提交 true, false, 默认true
    partitioner:   #分区选择模式 hash,random,round-robin, 默认hash
    version: #kafka版本, 默认2.8.1