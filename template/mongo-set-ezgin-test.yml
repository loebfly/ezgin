ezgin_mongo_set:
  xxx: # 唯一标识，用于获取连接时查找使用, 必填
    url: # mongo连接地址
    database: # mongo数据库名称
    pool_max: 20 #  连接池最大连接数 默认20
  yyy: # 唯一标识，用于获取连接时查找使用, 必填
    url: # mongo连接地址
    database: # mongo数据库名称
    pool_max: 20 #  连接池最大连接数 默认20